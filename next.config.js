/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: "/orhanaytekin.github.io",
  // other configurations...
}

module.exports = nextConfig